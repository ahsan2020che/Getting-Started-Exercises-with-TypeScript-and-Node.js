let usrname= ['admin','ahsan','hasnat','waleed']

for(let i=0;i<usrname.length || usrname.length>0;++i){
    if(usrname.length!=0){
    usrname.pop()}
}
if(usrname.length==0){
    console.log('We need to find some users!')
}